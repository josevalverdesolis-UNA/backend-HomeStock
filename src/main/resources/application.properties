# ==============================
#  SERVIDOR
# ==============================
server.port=${PORT:8080}
spring.application.name=homestock-backend

# ==============================
#  BASE DE DATOS (PostgreSQL en Render)
# ==============================
# Estas variables deben venir del entorno (Render o local)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================
#  JPA / HIBERNATE
# ==============================
# Valida que el esquema DB coincida con las entidades JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.default_schema=public

# ==============================
#  FLYWAY (habilitado)
# ==============================
spring.flyway.enabled=true
spring.flyway.locations=classpath:bd/migration

# ==============================
#  ACTUATOR
# ==============================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized
management.endpoints.web.base-path=/actuator

# ==============================
#  OPENAPI / SWAGGER-UI
# ==============================
springdoc.api-docs.path=/v1/api-docs
springdoc.swagger-ui.path=/swagger-ui
# ==============================
# JWT
# ==============================
# Se admite APP_JWT_SECRET como principal y JWT_SECRET como fallback para compatibilidad
app.jwt.secret=${APP_JWT_SECRET:${JWT_SECRET:change-this-in-prod-min-32-chars-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaa}}
app.jwt.issuer=homestock
# TTLs en segundos (por defecto: access 15min=900s, refresh 14d=1209600s)
app.jwt.access-ttl-seconds=${APP_JWT_ACCESS_TTL_SECONDS:900}
app.jwt.refresh-ttl-seconds=${APP_JWT_REFRESH_TTL_SECONDS:1209600}
