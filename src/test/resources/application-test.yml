spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/homestock
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none   # NO tocar esquemas en test
    properties:
      hibernate.format_sql: true
  sql:
    init:
      mode: never
logging:
  level:
    org.hibernate.SQL: warn
